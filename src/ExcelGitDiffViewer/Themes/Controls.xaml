<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!--
        コントロールの外観（Style / ControlTemplate）を集約する。色は必ず DynamicResource で
        Colors.Dark.xaml のトークンを参照し、この辞書自体は配色に依存しない。
        StaticResource で他 Style を参照する箇所があるため、被参照 Style を先に定義する。
    -->

    <!-- ============ Window（地色・既定文字色・フォント） ============ -->
    <!-- 暗黙スタイル（TargetType のみ）は WPF では型が完全一致する要素にしか適用されず、
         派生型の MainWindow / CommitPickerWindow には効かない。そのため x:Key を付け、
         各 Window から Style="{StaticResource AppWindowStyle}" で明示参照する。 -->
    <Style x:Key="AppWindowStyle" TargetType="Window">
        <Setter Property="Background" Value="{DynamicResource Bg.Window}" />
        <Setter Property="TextElement.Foreground" Value="{DynamicResource Fg.Primary}" />
        <Setter Property="FontFamily" Value="Yu Gothic UI, Meiryo" />
    </Style>

    <!-- ============ TextBlock（既定文字色） ============ -->
    <!--
        Window の TextElement.Foreground の継承だけでは、Border 等をまたいだ TextBlock が
        既定の黒文字になり暗背景で読めない（ファイルパス・詳細パネル・VBAコード行など）。
        暗黙スタイルで前景色を明示して視認性を担保する。個別に色指定した TextBlock
        （Fg.Secondary / Diff.*.Fg 等）はローカル値が優先されるため影響しない。
    -->
    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource Fg.Primary}" />
    </Style>

    <!-- 行番号列の値 TextBlock。上の暗黙スタイルを上書きし、控えめなグレーを維持する
         （code-behind の DataGridTextColumn.ElementStyle から参照）。 -->
    <Style x:Key="RowNumberTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource Grid.RowNumberFg}" />
    </Style>

    <!-- ============ Button（セカンダリ＝暗黙 / アクセント＝x:Key） ============ -->
    <Style TargetType="Button">
        <Setter Property="Foreground" Value="{DynamicResource Fg.Primary}" />
        <Setter Property="Background" Value="{DynamicResource Bg.Elevated}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Border.Default}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="12,5" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd" CornerRadius="6"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource Bg.SurfaceAlt}" />
                            <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource Border.Strong}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource Bg.Surface}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource Border.Focus}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="bd" Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- アクセントボタン（Trigger 内で TargetName 値を差し替えるため BasedOn せず独立定義） -->
    <Style x:Key="AccentButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="{DynamicResource Fg.OnAccent}" />
        <Setter Property="Background" Value="{DynamicResource Accent.Default}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="12,5" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd" CornerRadius="6"
                            Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource Accent.Hover}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource Accent.Pressed}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="bd" Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============ CheckBox（レビューモードトグル等） ============ -->
    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource Fg.Primary}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal" Background="Transparent">
                        <Border x:Name="box" Width="16" Height="16" CornerRadius="3"
                                Background="{DynamicResource Bg.Elevated}"
                                BorderBrush="{DynamicResource Border.Strong}" BorderThickness="1"
                                VerticalAlignment="Center">
                            <Path x:Name="check" Data="M 3,7 L 6.5,10.5 L 12.5,3.5" Stretch="None"
                                  Stroke="{DynamicResource Fg.OnAccent}" StrokeThickness="1.6"
                                  StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                  Visibility="Collapsed"
                                  HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="box" Property="Background" Value="{DynamicResource Accent.Default}" />
                            <Setter TargetName="box" Property="BorderBrush" Value="{DynamicResource Accent.Default}" />
                            <Setter TargetName="check" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="box" Property="BorderBrush" Value="{DynamicResource Accent.Hover}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============ ComboBox（表示モード切替等） ============ -->
    <!-- 既定の Aero テンプレはダーク時に白背景で浮くため、Button/CheckBox と同じテーマトークンで
         描画するカスタムテンプレを適用する。ドロップダウン Popup も枠・角丸・地色をテーマに合わせる。 -->
    <Style TargetType="ComboBoxItem">
        <Setter Property="Foreground" Value="{DynamicResource Fg.Primary}" />
        <Setter Property="Padding" Value="10,5" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Bd" Background="Transparent"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Bg.SurfaceAlt}" />
                        </Trigger>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Bg.SurfaceAlt}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Grid.CellSelectedBg}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="Foreground" Value="{DynamicResource Fg.Primary}" />
        <Setter Property="Background" Value="{DynamicResource Bg.Elevated}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Border.Default}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="Toggle" Focusable="False"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="Bd"
                                            Background="{DynamicResource Bg.Elevated}"
                                            BorderBrush="{DynamicResource Border.Default}"
                                            BorderThickness="1"
                                            CornerRadius="6">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <ContentPresenter Grid.Column="0" Margin="10,4,4,4" VerticalAlignment="Center" />
                                            <Path Grid.Column="1" Data="M 0,0 L 8,0 L 4,5 Z"
                                                  Fill="{DynamicResource Fg.Secondary}"
                                                  VerticalAlignment="Center" Margin="4,0,10,0" />
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Bg.SurfaceAlt}" />
                                            <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource Border.Strong}" />
                                        </Trigger>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource Border.Focus}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <ContentPresenter IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                          HorizontalAlignment="Left" VerticalAlignment="Center"
                                          Margin="10,0,26,0"
                                          TextBlock.Foreground="{DynamicResource Fg.Primary}" />
                        <Popup Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                            <Border Margin="0,4,0,0"
                                    Background="{DynamicResource Bg.Elevated}"
                                    BorderBrush="{DynamicResource Border.Default}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    MinWidth="{Binding ElementName=Toggle, Path=ActualWidth}"
                                    Padding="4">
                                <ScrollViewer MaxHeight="320">
                                    <ItemsPresenter />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============ ScrollBar（細いモダン） ============ -->
    <Style x:Key="ScrollBarThumbStyle" TargetType="Thumb">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border x:Name="thumbBd" CornerRadius="4" Margin="2"
                            Background="{DynamicResource ScrollBar.Thumb}" />
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="thumbBd" Property="Background" Value="{DynamicResource ScrollBar.ThumbHover}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ScrollBarPageButtonStyle" TargetType="RepeatButton">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border Background="Transparent" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Width" Value="12" />
        <Setter Property="MinWidth" Value="12" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid Background="{TemplateBinding Background}">
                        <Track x:Name="PART_Track">
                            <Track.Thumb>
                                <Thumb Style="{StaticResource ScrollBarThumbStyle}" />
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.PageDownCommand"
                                              Style="{StaticResource ScrollBarPageButtonStyle}" />
                            </Track.IncreaseRepeatButton>
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.PageUpCommand"
                                              Style="{StaticResource ScrollBarPageButtonStyle}" />
                            </Track.DecreaseRepeatButton>
                        </Track>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Orientation" Value="Vertical">
                            <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="True" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Width" Value="Auto" />
                <Setter Property="MinWidth" Value="0" />
                <Setter Property="Height" Value="12" />
                <Setter Property="MinHeight" Value="12" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ============ ListBox（VBA モジュール / コミット選択） ============ -->
    <!-- シートタブは MainWindow.xaml の ItemContainerStyle が優先されるため影響しない -->
    <Style TargetType="ListBox">
        <Setter Property="Background" Value="{DynamicResource Bg.Surface}" />
        <Setter Property="Foreground" Value="{DynamicResource Fg.Primary}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Border.Default}" />
    </Style>

    <Style TargetType="ListBoxItem">
        <Setter Property="Foreground" Value="{DynamicResource Fg.Primary}" />
        <Setter Property="Padding" Value="4,3" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="bd" Background="Transparent" Padding="{TemplateBinding Padding}">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource Bg.SurfaceAlt}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource Grid.CellSelectedBg}" />
                            <Setter Property="TextElement.Foreground" Value="{DynamicResource Fg.Primary}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============ GridSplitter ============ -->
    <Style TargetType="GridSplitter">
        <Setter Property="Background" Value="{DynamicResource Border.Strong}" />
    </Style>

    <!-- ============ ProgressBar ============ -->
    <Style TargetType="ProgressBar">
        <Setter Property="Foreground" Value="{DynamicResource Accent.Default}" />
        <Setter Property="Background" Value="{DynamicResource Accent.Muted}" />
        <Setter Property="BorderThickness" Value="0" />
    </Style>

    <!-- ============ DataGrid（差分グリッド） ============ -->
    <!-- セル余白を 0 にして、テンプレート内 Border が背景色をセル全面に塗れるようにする -->
    <Style x:Key="DiffCellStyle" TargetType="DataGridCell">
        <Setter Property="Padding" Value="0" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Background" Value="{DynamicResource Bg.Window}" />
        <Setter Property="Foreground" Value="{DynamicResource Fg.Primary}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">
                    <!-- セル自身が暗背景を塗る。差分色は内側テンプレートの Border が上に重ねる。
                         変更なしセル（内側 Border が透明）でも、この背景で暗く保たれる。 -->
                    <Border Background="{TemplateBinding Background}">
                        <ContentPresenter />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DiffColumnHeaderStyle" TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{DynamicResource Grid.HeaderBg}" />
        <Setter Property="Foreground" Value="{DynamicResource Grid.HeaderFg}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="BorderBrush" Value="{DynamicResource Grid.Line}" />
        <Setter Property="BorderThickness" Value="0,0,1,1" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridColumnHeader">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter VerticalAlignment="Center"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 行番号列セル（code-behind から FindResource で参照する） -->
    <Style x:Key="RowNumberCellStyle" TargetType="DataGridCell">
        <Setter Property="Background" Value="{DynamicResource Grid.RowNumberBg}" />
        <Setter Property="Foreground" Value="{DynamicResource Grid.RowNumberFg}" />
        <Setter Property="Padding" Value="6,2,8,2" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="HorizontalContentAlignment" Value="Right" />
    </Style>

    <!-- 左右共通の DataGrid 体裁。列は code-behind で動的生成する -->
    <Style x:Key="DiffGridStyle" TargetType="DataGrid">
        <Setter Property="AutoGenerateColumns" Value="False" />
        <Setter Property="IsReadOnly" Value="True" />
        <Setter Property="CanUserAddRows" Value="False" />
        <Setter Property="CanUserDeleteRows" Value="False" />
        <Setter Property="HeadersVisibility" Value="Column" />
        <Setter Property="SelectionUnit" Value="Cell" />
        <Setter Property="GridLinesVisibility" Value="All" />
        <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource Grid.Line}" />
        <Setter Property="VerticalGridLinesBrush" Value="{DynamicResource Grid.Line}" />
        <Setter Property="Background" Value="{DynamicResource Bg.Window}" />
        <Setter Property="RowBackground" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{DynamicResource Border.Default}" />
        <Setter Property="Foreground" Value="{DynamicResource Fg.Primary}" />
        <Setter Property="EnableColumnVirtualization" Value="True" />
        <Setter Property="EnableRowVirtualization" Value="True" />
        <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True" />
        <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling" />
        <Setter Property="VirtualizingPanel.ScrollUnit" Value="Pixel" />
        <Setter Property="CellStyle" Value="{StaticResource DiffCellStyle}" />
        <Setter Property="ColumnHeaderStyle" Value="{StaticResource DiffColumnHeaderStyle}" />
        <Setter Property="FontFamily" Value="Consolas, Yu Gothic UI, Meiryo" />
        <Setter Property="ColumnWidth" Value="SizeToCells" />
        <!-- 左右で行位置をピクセル単位に揃えるため行高を固定する。セルは単一行
             (TextTrimming=CharacterEllipsis) なので固定でも内容は欠けない。 -->
        <Setter Property="RowHeight" Value="24" />
    </Style>
</ResourceDictionary>
